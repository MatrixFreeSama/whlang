program rankn_axis_erasure
strict
input n: u64 range 4..1024
axis i in n
axis j in 4
region compute effect pure parallel {
    for i in 0..n, j in 0..4 {
        total += cast(f64, i)
    }
}
output total
test (4) => { total = 24.0 }
